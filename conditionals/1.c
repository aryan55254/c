//if => executes code if condiiton is true 
#include <stdio.h>

int main() {
    int num = 10;

    if (num > 0) {  // Condition is true
        printf("Number is positive.\n");
    }

    return 0;
}
