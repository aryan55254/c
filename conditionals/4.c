//switch case 
switch (expression) {
    case value1:
        // Code executes if expression == value1
        break;
    case value2:
        // Code executes if expression == value2
        break;
    ...
    default:
        // Executes if no cases match
}
