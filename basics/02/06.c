//internal 
#include <stdio.h>
static int z = 10;
void main(){
    printf ("x here can't be accesed outside this file");
}
// intmain() and void main() can be used as entry points for a program but int is prefereed wby because void says it results in null value 