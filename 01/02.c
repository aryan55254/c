#include <stdio.h>
int main(){
    //int stores integers(whole no.s) without decimals such as 123 / -123
    //float declared no,s with decimals 
    // chars tores characters in single quotes
    int myint = 15;
    int secondint;
    secondint = 44;
    //format specifiers are used to tell compiler what type os data variable is storing 
    //%d for int
    // %c for char
    //%f for float
    printf("%d\n",myint);
    printf("%d\n",secondint);
    return 0;
}