#include <stdio.h>
int main()
{
    //int char double float 
    int age = 24 ;
   // printf("my age is" + age); this wont work in c u have to provide formatting
   printf ("my age is %d\n" , age );
  
    char firstnamech = 'l';
    printf ("my first character is %c" , firstnamech);
    
    //in array u can store contagious elemets of same data type
    //int = 4 bytes 
    //uint = insigned int is only positive 
    //signed is first bt used for sign can be negative or positive
    return 0;
}
